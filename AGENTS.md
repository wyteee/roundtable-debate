# AGENTS.md ——《圆桌》深夜辩论电台

> 本文件供 AI 编程协作(Claude Code / Cursor 等)读取，用于对齐产品逻辑与工程约定。
> 完整产品需求见 PRD v2.0；本文件只保留"写代码必须知道"的浓缩信息，改动优先以 PRD 为准。

## 1. 产品一句话

用户写下一个人生问题，3 位历史名人的"电子灵魂"（从 8 位固定人物卡中选出）围坐圆桌为其辩论，用户全程可插话/终止，结束后生成结辩高光分享卡。

## 2. 技术栈（不要偏离，除非用户明确要求换）

- 前端：React + Vite + TypeScript + Tailwind
- 后端：Vercel Functions（或 Cloudflare Workers），仅 1 个接口 `POST /api/debate`，SSE 流式返回
- LLM：OpenAI 兼容协议，主力 DeepSeek，调试期可用 GLM-4.7-Flash，备选升级 Claude
- 分享卡导出：html2canvas（纯前端）
- 数据库：无。无账号体系，session 结束即清空
- 字体：标题 Press Start 2P；正文 Space Grotesk；系统提示/标签 Space Mono
- 配色：深空底 `#0B0616 → #150A2E` 渐变；霓虹紫 `#9D4EF6`、品红 `#FF2EC9`、青 `#00F0FF`

## 3. 不可违反的三条设计原则

1. **人格即立场**：绝不写"你扮演反方"这类导演式指令。立场必须从人物卡（creed/voice/arsenal）自然涌现。
2. **规则只保护人格，不产生内容**：报警器、字数限制等系统机制只是防止人格崩坏，不能替角色说话或强加观点。
3. **用户是主持人**：用户拥有随时终止权和插话权，且不强制其操作。

## 4. 人物卡字段规范（唯一的内容生产装置）

写在 `characters/*.json`，每个角色必须包含以下字段（按重要性排序）：

| 字段 | 说明 |
|---|---|
| example_dialogues | 3-5 段手写台词，定语气，防 AI 腔的最强手段 |
| taboos | 禁忌清单，反面约束优先于正面描述 |
| relations | 对其余 7 位角色的看法/张力点 |
| arsenal | 5-8 条核心论点 + 一句原话/思想锚点 |
| bio | 约 200 字，只写塑造观点的创伤/转折 |
| voice | 句长、修辞、是否讽刺、锚点口诀 |
| creed | 该角色在本产品中的"工作职责"（例：检验用户的问题） |

改人物卡 = 改产品。一次迭代目标 10 分钟内完成，不需要 RAG，不需要蒸馏著作。

**最终 8 位人物名单（已确认，替换了 PRD 原稿中的 5 位）：**

| 人物 | 状态 |
|---|---|
| 尼采 | 沿用 PRD 原案 |
| 马基雅维利 | 替换苏格拉底 |
| Steve Jobs | 替换爱比克泰德（已故公众人物，可正常编写台词） |
| 西蒙娜·波伏娃 | 沿用 PRD 原案 |
| 查理·芒格 | 沿用 PRD 原案 |
| 苏轼 | 替换老子 |
| 拿破仑 | 替换卡尔·荣格 |
| 卡尔·马克思 | 替换简·奥斯汀 |

> 换人原则：只能是**已故历史/公众人物**，不能是在世真人（如 Elon Musk、Sam Altman 这类请求已被拒绝）——人物卡机制需要编写"原话/思想锚点"和 example_dialogues，安在在世真人身上属于虚构真人发言，不做。已故人物（含有争议但非暴行符号级别的，如马克思、拿破仑）可以正常处理。

MVP 必须完整打磨的 5 位：尼采、**马基雅维利**、**Steve Jobs**、波伏娃、芒格。
第二批扩展（MVP 后）：苏轼、**拿破仑**、**卡尔·马克思**。

## 5. 核心机制（写代码时严格遵守）

- **轮次**：开场轮（第 0 轮，三人并行不计数）→ 标准交锋第 1-4 轮 → 延长交锋第 5-9 轮（仅用户主动继续才发生）→ 第 10 轮硬上限强制收尾。
- **发言硬限**：每条发言 ≤150 字。
- **私有记忆**：每轮每个角色额外生成一句"内心活动"存入 `private_memory[角色]`，只喂给自己，不展示给用户。
- **用户插话特殊指令**：插话后，下一位发言者收到指令——"先指出插话观点中最脆弱的假设并攻击，若部分同意需先说明不同意的部分"。
- **烟雾报警器**（后台隐藏，永不产生对用户可见的内容，故障才介入）：
  - 判定"趋同" → 给下一位发言者悄悄注入"若你的真实立场与刚才发言冲突，现在必须说出来"
  - 判定"绕圈" → 触发"建议收尾"横幅，用户拍板，不强制跳转
  - 反向质量指标：触发次数越少越好（≤1 次/场为验收标准）
- **结辩高光**：禁止发起额外的"总结陈词"LLM 调用。规则打分选出该角色本场最强 1-2 句发言，用一次轻量压缩改写生成 ≤40 字忠告，禁止说本场没说过的新观点。
- **会话/隐私**：每场辩论 = 独立 session，结束即清空，不跨 session 留存，无账号体系，刷新页面即作废（MVP 不做断线续传）。

## 6. 接口约定

`POST /api/debate`
- 入参：`question`（string）+ `characterIds`（长度为 3 的数组）+ 会话状态（用于插话/继续等后续请求）
- 出参：SSE 流，逐条推送发言（含角色名、正文、可选的反驳标签）、报警器横幅事件、收尾高光事件
- API Key 一律走服务端环境变量，绝不进入前端代码或仓库

## 7. Scope Out（明确不做，别自作主张加）

- PixiJS 或任何游戏引擎场景
- 账号系统、历史云同步
- 8 位以上的更多角色（本版本已从 5 扩到 8，到此为止）
- 语音交互、角色语音合成
- 单角色 SFT（除非 badcase 触发条件满足）
- RAG / 知识库接入
- CRT 扫描线/VHS 噪点滤镜（MVP 后再做，不阻塞上线）

## 8. 验收标准（改完代码/人物卡后自查）

- 三人开场立场互不重复，可追溯回各自世界观（无导演分配）
- 至少一次自发反驳或结盟（非指令强制）
- 用户插话后至少一人指出假设漏洞而非附和
- 全程无"作为 AI""值得深思""首先其次"类 AI 腔
- 报警器触发次数 ≤1 次/场
- 标准场景 3-4 轮内自然收敛
- 结辩高光内容可在对话历史中找到出处
- 分享卡可正常导出 PNG
- 5 位 MVP 角色"不像他"率 <30%

## 9. 给 AI 协作者的提醒

- 遇到"要不要加一个更聪明的兜底机制"这类冲动，先看第 3 条设计原则——大概率不该加。
- 人物卡质量问题优先改字段本身（尤其 taboos 和 example_dialogues），不要试图用额外的系统指令去"修正"人格表现。
- 任何新增机制如果会产生用户不可见但影响角色发言内容的强制指令，需先确认不违反"规则只保护人格，不产生内容"。
