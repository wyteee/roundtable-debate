## 2026-08-31（续2）

### 新功能：用户插话正式接入，三位嘉宾会真实回应

基于之前重构的按轮次请求架构（方案A），把PRD 3.4节"用户插话特殊处理"机制从
纯前端UI展示，落地为真正影响后端生成内容的功能。开发过程中经过多轮实测反馈，
迭代修复了以下问题：

**交互模式：从"一直开着的输入框"改成"我要插话"按钮**
- 原因：自由输入框在辩论持续自动推进的情况下，用户打字期间后端可能已经悄悄
  跑完了好几轮，导致插话内容被塞进"当时刚好在跑的那一轮"，出现插话内容与
  实际回应错位好几轮的问题
- 改动：新增显式的"我要插话"按钮。点击后通过`interjectionPausedRef`真正暂停
  整个自动推进流程（不自动请求下一轮、不弹报警器横幅），直到用户提交或点击
  "算了，继续吧"放弃，才通过`resumeTick`状态强制watcher effect重新评估、恢复流程
- 已知边界：如果点击按钮那一刻恰好有一轮请求正在传输中，那一轮无法收回、会
  照常完成；但只要用户还未提交/放弃，后续轮次会正确等待，不会像之前那样被
  更远的轮次抢走

**固定缓冲期，避免"发言越短、反应时间越短"**
- 原设计里，能点插话按钮的窗口完全依附于"这一轮发言播放需要多久"，导致
  AI回复变短时反应窗口跟着缩水，出现"点了也来不及"的体验问题
- 新增`POST_ROUND_GRACE_MS`常量，每轮播放完毕后固定追加一段独立缓冲期
  （不管发言长短），期间按钮变红并提示"现在正好可以"。经反馈从3秒调至6秒

**插话持久化进对话记录，而非用完即忘**
- 原设计里插话内容只作为"特殊指令"临时塞给本轮第一位发言者，从未真正写入
  `history`，导致同一轮的其他两位发言者的上下文里根本看不到用户说了什么——
  不是他们选择无视，是压根不可见；且下一轮开始后，这句话就从系统记忆里彻底消失
- 修复：把插话包装成一条`{charId:"user", name:"你", text}`的历史记录，插入
  `workingHistory`，让本轮全员可见；同时通过响应里的`interjectionEntry`字段
  传回前端、并入`historyRef`持久保存，之后所有轮次的请求都会带着这条记录

**发言顺序按轮次轮换，不再永远固定同一个人**
- 原实现里每轮发言顺序都是`characterIds`数组的固定顺序，导致"处理插话"这个
  职责永远只会落在数组里排第一位的角色身上，另外两位无论辩论多少轮都不会
  被赋予正面回应用户的机会，跟PRD 3.5节"自然轮换"的表述相悖
- 修复：按`(round-1) % 角色数`做循环位移，三人轮流"打头阵"

**插话回应的prompt结构改为强制三段式**
- 原设计里，处理插话的发言者依然会同时看到"方式A反驳/方式B推进/方式C结盟"
  菜单和插话指令，两套指令同时给容易让模型优先响应菜单、把插话当次要内容
- 改为完全独立的模板：第一段必须正面回应插话（指出最脆弱假设并攻击，或说明
  部分同意部分不同意）；第二段可选，视情况呼应其他嘉宾；第三段收在对问题
  本身的清晰判断上，不再提供A/B/C菜单

**收尾阶段并行化**
- `phase: "closing"`生成三人忠告时，原实现跟当初的开场轮同样的错误——顺序
  执行三次Claude调用，感觉像"卡住"，实际是三次调用时间顺序相加。改成`Promise.all`
  并行后明显加快

**修复：`idRef`遗漏声明导致运行时崩溃**
- 排查一次"第一个嘉宾开场都没说完就整个应用崩溃"的问题，浏览器控制台显示
  `Uncaught ReferenceError: idRef is not defined`。定位为此前几轮重构state时
  手滑漏掉了`const idRef = useRef(0)`的声明行，但引用`idRef.current`生成消息
  唯一id的代码仍留在4处，导致第一条消息要生成id时立即抛出运行时错误。
  `node --check`等静态语法检查无法发现此类"变量未声明但被使用"的问题，
  只有实际运行才会暴露——后续遇到类似"改完语法检查通过但运行崩溃"的情况，
  应优先要浏览器控制台的实际报错文本，而非继续静态审查代码

### 涉及文件
- `api/debate.js`：插话持久化、发言顺序轮换、插话回应prompt改造、收尾并行化
- `src/sections/DebateScreen.tsx`：插话按钮交互重做、固定缓冲期、`idRef`声明修复
# 改动日志 · 圆桌 Roundtable

> 每次 Claude 协助改动代码/人物卡/脚本，都会在这里加一条新记录，方便随时核对"改了什么、为什么改"。
> 格式：日期 + 改动摘要 + 涉及文件 + 原因（如果是修bug，注明触发条件）

---

## 2026-08-30

### 项目初始化
- 建立仓库骨架：`api/` `src/` `characters/` `public/` 目录结构
- 新建 `AGENTS.md`：产品逻辑与工程约定对齐文档
- 新建 `.gitignore` `.env.example` `README.md`

### 人物名单确定
- 最终8位：尼采、马基雅维利（替苏格拉底）、Steve Jobs（替爱比克泰德）、波伏娃、芒格、苏轼（替老子）、拿破仑（替荣格）、卡尔·马克思（替简·奥斯汀）
- 同步更新 `AGENTS.md` 人物名单表

### 8张人物卡完成（`characters/*.json`）
- `nietzsche.json` `machiavelli.json` `jobs.json` `beauvoir.json` `munger.json` MVP 5张
- `su_shi.json` `napoleon.json` `marx.json` 扩展3张
- 修订：苏轼补充苏辙/苏洵/交游线（原版遗漏，直接影响其"心安处是吾乡"世界观的重要支撑）；马克思删除私生子细节（与其经济学论证无实质关联），改为议会"蓝皮书"取证方式+陪女儿玩角色扮演游戏+莎士比亚爱好（与其严谨论证风格/私下温情面更直接相关）

### 测试脚本迭代（`scripts/test-debate.mjs`）
- 从0搭建：读人物卡拼system prompt，跑开场轮+交锋轮，调用Claude API
- 修复：`max_tokens`从400→700→1000（中文token开销+JSON格式开销导致多次截断）
- 修复：交锋轮指令改为"只能回应一人一句话，不能同时怼多人"（避免发言过长+跑题）
- 新增：结尾必须重申问题本身立场的硬性要求（防止讨论滑向纯案例拉锯）
- 新增：私有记忆机制（每次发言同时产出公开发言+内心活动，后者只喂给角色自己）
- 修复：私有记忆指令去掉"情绪必须累积升级"的偏向性表述，改为如实反映当下情境
- 输出格式几经调整：JSON → 分隔符标记 → 改回JSON+禁止英文直引号+正则抢救兜底（角色发言爱用引号强调，与JSON转义冲突，最终方案是禁止模型使用英文直引号）
- 新增：方式C"结盟"选项 + relations字段里正面认可部分要认真对待的提醒（此前relations多为"认可+但+批评"句式，导致辩论清一色互怼、从未出现结盟）
- 新增：完整轮次控制（标准3-4轮，可用`DEBATE_ROUNDS`环境变量调）+ 收尾高光生成（规则打分选句`pickTopLines` + 轻量压缩改写`compressToAdvice`，不发起新的总结陈词LLM调用）
- 新增：题目支持`DEBATE_QUESTION`环境变量覆盖，角色组合支持命令行参数指定

### 正式后端接口（`api/debate.js`）
- 新建：把`test-debate.mjs`验证过的核心逻辑包装成正式的`POST /api/debate`，Vercel Serverless Function，SSE流式返回
- 新增：`response_type`（rebut/ally/advance）+ `target`结构化输出，供后端拼出前端需要的`tag`字符串（如"反驳 尼采"），对应PRD"标签来自系统分类，不改变角色语言本身"的要求
- SSE事件类型：`speech`（发言）、`banner`（建议收尾）、`advice`（收尾高光）、`done`（结束）、`error`（出错）
- 已知待办：用户实时插话机制暂未接入（PRD本身也把这个列为MVP可选项），当前只支持"我已有答案"按钮直接中断连接跳转收尾屏

### 前端（Kimi生成，非Claude产出）
- 技术栈对齐AGENTS.md：React + Vite + TS + Tailwind + html2canvas
- 视觉方向由用户另行确定为"柯布西耶风格"（非PRD原定的深空霓虹未来复古风，为用户主动选择的新方向）
- 已知问题：`DebateScreen.tsx`目前是完全预写死的剧本（`characters.ts`里`opening`/`clashes`/`replyUser`/`advice`都是固定文本），需要重构为消费`/api/debate`的真实流式数据，这是下一步工作
